import Foundation

enum Rotation {

    static func align(_ bits: [UInt8]) -> [UInt8] {
        // v1: identity
        bits
    }

    static func restore(_ bits: [UInt8]) -> [UInt8] {
        // inverse of align
        bits
    }
}
